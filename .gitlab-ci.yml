stages:
  - test

variables:
  GO_VERSION: "1.27"

test:
  stage: test
  image: golang:${GO_VERSION}
  script:
    - go get -d -t -v ./...
    - go test -v ./...
    - go build -v ./...
    - cd testing
    - sh run_gitlab_tests.sh
